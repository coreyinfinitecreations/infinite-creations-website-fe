# Client login uses the same-origin /api/client proxy.
# Local proxy target is http://localhost:3002 (apps/manage).
# Production routing is configured in vercel.json.
# Supabase and Stripe credentials belong only in the manage backend environment.
